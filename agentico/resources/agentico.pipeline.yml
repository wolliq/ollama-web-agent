# The main pipeline for agentico
resources:
  pipelines:
    agentico_pipeline:
      name: agentico_pipeline
      catalog: workspace
      schema: agentico_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
